name: Build Anywhere Teleport v0.2.0

on:
  push:
    branches:
      - build-anywhere-storage-20260902
    paths:
      - 'tools/anywhere-teleport/**'
      - '.github/workflows/build-anywhere-teleport-temp.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Download BepInEx IL2CPP references
        shell: bash
        run: |
          set -euo pipefail
          rm -rf tools/anywhere-teleport/lib tools/anywhere-teleport/refs
          mkdir -p tools/anywhere-teleport/lib tools/anywhere-teleport/refs
          curl -L --fail --retry 3 \
            'https://github.com/BepInEx/BepInEx/releases/download/v6.0.0-pre.2/BepInEx-Unity.IL2CPP-win-x64-6.0.0-pre.2.zip' \
            -o /tmp/bepinex.zip
          unzip -q /tmp/bepinex.zip -d tools/anywhere-teleport/lib
          chmod -R u+rwX tools/anywhere-teleport/lib
          for f in 0Harmony.dll BepInEx.Core.dll BepInEx.Unity.IL2CPP.dll Il2CppInterop.Runtime.dll; do
            src="$(find tools/anywhere-teleport/lib -type f -name "$f" | head -n 1)"
            test -n "$src"
            cp "$src" "tools/anywhere-teleport/refs/$f"
          done

      - name: Build plugin
        shell: bash
        run: |
          set -euo pipefail
          dotnet build tools/anywhere-teleport/HinataS1AnywhereTeleport.csproj -c Release

      - name: Stage artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact
          cp tools/anywhere-teleport/bin/Release/net6.0/HinataS1AnywhereTeleport.dll artifact/
          sha256sum artifact/HinataS1AnywhereTeleport.dll > artifact/SHA256SUMS.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: HinataS1AnywhereTeleport-v0.2.0
          path: artifact/
          if-no-files-found: error
          retention-days: 7
